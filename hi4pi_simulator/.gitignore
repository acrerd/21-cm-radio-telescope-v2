# HI4PI data files (auto-downloaded on first run by hi4pi_data.py)
*.fits
*.download
*.tmp

# nhi_grid_cache.npy and hi4pi_compact.npz.xz are small derived data
# files committed on purpose, so a fresh clone needs no downloads

# script outputs
spectrum_*.png
spectrum_*.txt

# tooling
__pycache__/
.mypy_cache/
